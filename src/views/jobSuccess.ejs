<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Job Posted Successfully | AI Resume Screener</title>
    <meta http-equiv="refresh" content="3; url=/" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <style>
      :root {
        --primary: #4361ee;
        --secondary: #3a0ca3;
        --accent: #4cc9f0;
        --success: #38b000;
        --light: #f8f9fa;
        --dark: #212529;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Poppins", sans-serif;
      }

      body {
        background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 20px;
        text-align: center;
      }

      .success-container {
        background: white;
        border-radius: 20px;
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
        padding: 50px 40px;
        width: 100%;
        max-width: 600px;
        position: relative;
        overflow: hidden;
      }

      .success-container::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 10px;
        background: linear-gradient(90deg, var(--success), var(--accent));
      }

      .checkmark-circle {
        width: 100px;
        height: 100px;
        background: linear-gradient(135deg, var(--success) 0%, #38b000 100%);
        border-radius: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
        margin: 0 auto 25px;
        box-shadow: 0 5px 15px rgba(56, 176, 0, 0.3);
        animation: scaleIn 0.5s ease-out;
      }

      .checkmark {
        color: white;
        font-size: 50px;
      }

      h2 {
        font-size: 1.8rem;
        margin-bottom: 15px;
        color: var(--dark);
      }

      .job-title {
        color: var(--primary);
        font-weight: 600;
        display: inline;
      }

      p {
        font-size: 1.1rem;
        color: #495057;
        margin-bottom: 25px;
        line-height: 1.6;
      }

      .redirect-info {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
        margin-bottom: 25px;
        color: #6c757d;
        font-size: 0.95rem;
      }

      .spinner {
        width: 20px;
        height: 20px;
        border: 3px solid rgba(67, 97, 238, 0.2);
        border-top-color: var(--primary);
        border-radius: 50%;
        animation: spin 1s linear infinite;
      }

      .btn {
        display: inline-block;
        background: linear-gradient(
          135deg,
          var(--primary) 0%,
          var(--secondary) 100%
        );
        color: white;
        padding: 12px 25px;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 5px 15px rgba(67, 97, 238, 0.3);
        margin-top: 10px;
      }

      .btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 20px rgba(67, 97, 238, 0.4);
      }

      .btn:active {
        transform: translateY(0);
      }

      .ai-elements {
        position: absolute;
        opacity: 0.1;
        z-index: -1;
        font-size: 80px;
      }

      .ai-1 {
        top: 20px;
        right: 30px;
      }

      .ai-2 {
        bottom: 20px;
        left: 30px;
      }

      @keyframes scaleIn {
        0% {
          transform: scale(0);
          opacity: 0;
        }
        80% {
          transform: scale(1.1);
        }
        100% {
          transform: scale(1);
          opacity: 1;
        }
      }

      @keyframes spin {
        to {
          transform: rotate(360deg);
        }
      }

      @media (max-width: 768px) {
        .success-container {
          padding: 40px 25px;
        }

        h2 {
          font-size: 1.5rem;
        }

        .checkmark-circle {
          width: 80px;
          height: 80px;
        }

        .checkmark {
          font-size: 40px;
        }
      }
    </style>
  </head>
  <body>
    <div class="success-container">
      <div class="ai-elements ai-1">🤖</div>
      <div class="ai-elements ai-2">📊</div>

      <div class="checkmark-circle">
        <i class="fas fa-check checkmark"></i>
      </div>

      <h2>
        Job "<span class="job-title"><%= job.title %></span>" Posted
        Successfully!
      </h2>
      <p>
        Your job listing is now active and our AI is scanning potential
        candidates.
      </p>

      <div class="redirect-info">
        <div class="spinner"></div>
        <span>You will be redirected in 3 seconds</span>
      </div>

      <a href="/" class="btn">
        <i class="fas fa-home"></i> Return to Homepage
      </a>
    </div>
  </body>
</html>
